{
  "name": "tesoro-maldito",
  "version": "1.0.0",
  "description": "Proyecto en TypeScript orientado a objetos",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "postbuild": "cp index.html dist/ && cp -r assets dist/",
    "build:vercel": "npx tsc && cp index.html dist/ && cp -r assets dist/",
    "dev": "npx tsc --watch",
    "serve": "npx http-server . -p 8080 -o",
    "start": "npm run build && npm run serve"
  },
  "keywords": ["game", "treasure", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
